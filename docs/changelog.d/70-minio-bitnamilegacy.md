- **Compose and Lite pull MinIO from `docker.io/bitnamilegacy` pinned by index digest (#70).** `deploy/compose/docker-compose.yml` and `deploy/lite/Makefile` now reference `bitnamilegacy/minio:2025.7.23-debian-12-r5` and `bitnamilegacy/minio-client:2025.7.21-debian-12-r3` by multi-arch digest: MinIO's own images are closed to anonymous pulls (`quay.io/minio/*` and `docker.io/minio/*` both answer 401), so a fresh `docker compose up` / `make -C deploy/lite up` could not start. The Bitnami images run as uid 1001 behind their own entrypoint, so MinIO is launched as root with `minio` as the entrypoint and `mc` gets `MC_CONFIG_DIR=/tmp/.mc`. One regression to note: this MinIO is **2025-07-23**, older than the 2025-09-07 pin it replaces — CI/dev only, no cluster runs MinIO. See [Deployment](/deployment).
