- **Compose and Lite pull MinIO from quay.io at pinned release tags (#61).** `deploy/compose/docker-compose.yml` and `deploy/lite/Makefile` now reference `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z` and `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z`; the Docker Hub `minio/*` repositories return "pull access denied", so a fresh `docker compose up` / `make -C deploy/lite up` could not start. No configuration change for self-hosters. See [Deployment](/deployment).
