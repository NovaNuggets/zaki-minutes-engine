- **Helm chart pulls MinIO from quay.io at pinned release tags (#68).** `deploy/helm/charts/vexa` now references `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z` and `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z`, matching the compose/lite pins from #61 — the Docker Hub `minio/*` repositories return "pull access denied", so a fresh Helm install could not start MinIO. No configuration change for self-hosters.
