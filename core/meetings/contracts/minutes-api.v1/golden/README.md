# minutes-api.v1 golden vectors

The positive files cover capture, withdrawal, public status, both supported error shapes
(including the stable `meeting_url_invalid` denial),
and meeting/account erasure receipts. `.invalid-` files prove that internal status vocabulary
and surplus sensitive fields do not cross the exact public boundary. Adjacent database ids above
JavaScript's safe-integer ceiling remain distinct strings; JSON numbers and bigint overflow fail.
