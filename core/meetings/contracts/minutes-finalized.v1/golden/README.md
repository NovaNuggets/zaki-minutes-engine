# minutes-finalized.v1 goldens

- `Envelope.meeting.json` is the exact content-free finalized-transcript event for meeting row 41.
- `SignatureHeaders.signed.json` is the exact HMAC-only header set; no bearer credential is allowed.
